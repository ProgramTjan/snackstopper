# Flask
SECRET_KEY=change-me-to-a-random-string

# VAPID keys for Web Push (generate with: python -c "from pywebpush import webpush; from py_vapid import Vapid; v=Vapid(); v.generate_keys(); print('PRIVATE:', v.private_pem()); print('PUBLIC:', v.public_key)")
# Or run: python generate_vapid.py
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_CLAIMS_EMAIL=mailto:you@example.com

# Defaults
DEFAULT_REMINDER_TIME=16:50
DEFAULT_AVERAGE_AMOUNT=7.50
